FROM postgres:10
RUN mkdir -p "$PGDATA" && chmod 700 "$PGDATA"
EXPOSE 5432
#ENTRYPOINT [ "/bin/bash", "-l", "-c" ]
#CMD ["-c", "pg_createcluster 10 main && pg_ctlcluster 10 main start && pg_lsclusters"]







